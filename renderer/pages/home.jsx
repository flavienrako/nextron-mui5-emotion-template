import React from "react";
import { Button, Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Typography css={{ color: "red" }}>Title</Typography>
      <Button css={{ background: "tomato" }}>Test</Button>
    </div>
  );
};

export default Home;
